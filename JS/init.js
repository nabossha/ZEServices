/**
 * Created by nabossha on 01.09.2015.
 */
/*
$(function(){
    $('#page').xpull({
        'spinnerTimeout':10000,
        'callback':function(){
            //$("DIV.values").empty();
            //$("DIV.values").html('<div class="loadupdate centered">aktualisiere...</div>');
            setTimeout(function(){// wait
                var scope = angular.element($("body")).scope();
                scope.$apply(function () {
                    scope.loadServerData("refresh");
                });

                //location.reload(); // reload the page
            }, 350);
        }
    });
});

*/

